import styled from 'styled-components';

const Username = styled.a`
  margin-left: 5px;
  font-size: 1.5rem;
  color: currentColor;
  cursor: pointer;

  &:hover {
    color: #98C379;
  }
`;

export default Username;
